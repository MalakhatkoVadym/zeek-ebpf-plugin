[package]
description = This plugin provides native AF_XDP support for Zeek.
tags = zeek plugin, packet source, af_xdp
plugin_dir = build/itimmer_af_xdp.tgz
build_command = ./configure --zeek-dist=%(zeek_dist)s && make
test_command = cd tests && btest -d
depends =
  zeek-pkg >=1.2
  zeek >=2.5.0
